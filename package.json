{
  "name": "brain-republic",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "두뇌공화국: 면접 대작전 — NAN 2026 (NHN Game × AI Hackathon) 사전 과제 출품작",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist --add",
    "docs": "node scripts/make-pdfs.mjs",
    "deploy:unity": "gh-pages -d unity/unity-build --dest unity --add"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "marked": "^16.0.0",
    "playwright-core": "^1.55.0",
    "vite": "^7.0.0"
  }
}
